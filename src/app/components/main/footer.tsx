"use client"

import Image from "next/image"

const Footer = () => {
    return (
        <footer className="relative border-t border-t-slate-300 bg-gren-500 text-white w-full h-max justify-center items-center bg-green-500 px-6 py-4 flex">
            <p>Terima kasih sudah membaca al-quran</p>
        </footer>
    )
}

export default Footer

